#include <stdio.h>

int main()
{
    char s;
    int char[26] = {0};
    while (scanf("%c" &c") != EOF) {
        if (c >= 'a' && c <= 'z')
    {
        cnt[c - 'a';]
    }
}
for (char i = 'a'; i <= 'z'; i++)
    if (cnt[i] > 0)
    {
        printf("%c - %d\n", i, cnt[i - 'a']);
    }

return 0;
}
